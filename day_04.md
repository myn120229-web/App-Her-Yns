# روز ۴ — یکشنبه ۴ اکتبر ۲۰۲۶

## هدف امروز
راه‌اندازی محیط توسعه و اجرای موفق تست‌ها به‌صورت محلی.

## تسک‌های امروز (حدود ۳/۵ ساعت)
1. **(۶۰ دقیقه)** برای هر پروژه هدف: Fork کن، سپس clone محلی، ساخت virtualenv و نصب وابستگی‌ها.
2. **(۶۰ دقیقه)** اجرای مجموعه تست‌ها (pytest) به‌صورت محلی تا مطمئن شوی همه سبز (Pass) هستند. اگر خطایی بود، دقیقاً یادداشت کن و در کانال پروژه بپرس.
3. **(۴۵ دقیقه)** اجرای لینتر و فرمت‌کننده (ruff / black / mypy بر اساس تنظیمات پروژه) و آشنایی با تنظیمات pre-commit.
4. **(۴۵ دقیقه)** ساخت شاخه (Branch) آزمایشی و اجرای کامل چرخه: تغییر کوچک → commit → push → ساخت Pull Request آزمایشی → بستن آن (تمرین).

## خروجی امروز
- هر ۲-۳ پروژه هدف به‌صورت محلی اجرا و تست شده‌اند
- یک PR آزمایشی بسته‌شده (برای تمرین فرایند)

## یادداشت
- اگر نصب وابستگی‌ها خطا داد، اول در Issues مخزن جست‌وجو کن؛ معمولاً راه‌حل وجود دارد.
